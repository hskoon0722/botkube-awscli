name: Deploy Botkube plugins on GitHub Release

on:
  push:
    tags:
      - "*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # 실제로 dist/ 아래 실행파일을 빌드합니다 (스냅샷 이름: executor_aws_linux_*)
      - name: Build plugins
        uses: goreleaser/goreleaser-action@v6
        with:
          version: latest
          args: build --clean --snapshot

      # 릴리즈 자산을 가리키는 index 생성 (정확한 들여쓰기 보장)
      - name: Generate plugins-index.yaml
        shell: bash
        run: |
          set -Eeuo pipefail
          TAG="${GITHUB_REF#refs/tags/}"
          BASE="https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}"
          echo "URL BASE: $BASE"
          ls -l dist || true

          NAME="aws"
          TYPE="executor"
          DESC="Run AWS CLI from chat."
          VERSION="0.1.0"

          {
            printf "entries:\n"
            printf "  - name: %s\n" "$NAME"
            printf "    type: %s\n" "$TYPE"
            printf "    description: %s\n" "$DESC"
            printf "    version: %s\n" "$VERSION"
            printf "    urls:\n"
            if [[ -f dist/executor_aws_linux_amd64 ]]; then
              printf "      - url: %s/executor_aws_linux_amd64\n" "$BASE"
              printf "        platform:\n"
              printf "          os: linux\n"
              printf "          architecture: amd64\n"
            fi
            if [[ -f dist/executor_aws_linux_arm64 ]]; then
              printf "      - url: %s/executor_aws_linux_arm64\n" "$BASE"
              printf "        platform:\n"
              printf "          os: linux\n"
              printf "          architecture: arm64\n"
            fi
            printf "    jsonSchema:\n"
            printf "      value: |-\n"
          } > plugins-index.yaml

          # JSON 본문은 8칸 들여쓰기 적용
          sed 's/^/        /' >> plugins-index.yaml <<'JSON'
          {
            "$schema":"http://json-schema.org/draft-04/schema#",
            "title":"aws",
            "type":"object",
            "properties":{
              "defaultRegion":{"type":"string"},
              "prependArgs":{"type":"array","items":{"type":"string"}},
              "allowed":{"type":"array","items":{"type":"string"}},
              "env":{"type":"object","additionalProperties":{"type":"string"}}
            },
            "additionalProperties": false
          }
          JSON

          echo "    recommended: false" >> plugins-index.yaml

          echo "----- GENERATED INDEX -----"
          sed -n '1,200p' plugins-index.yaml

          # (안전장치) YAML 파싱 확인: 여기서 실패하면 봇까지 안 갑니다.
          python3 - <<'PY'
          import yaml, sys
          with open('plugins-index.yaml', 'rb') as f:
              yaml.safe_load(f)
          print('YAML looks valid.')
          PY

      - name: Create or update GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -Eeuo pipefail
          TAG="${GITHUB_REF#refs/tags/}"
          # 릴리즈가 없으면 만들고, 있으면 그대로 진행
          gh release view "$TAG" >/dev/null 2>&1 || gh release create "$TAG" -t "$TAG" -n "Botkube plugins $TAG"
          # 자산 업로드 (덮어쓰기 허용)
          gh release upload "$TAG" dist/executor_* plugins-index.yaml --clobber

      - name: Show index URL
        run: echo "Index URL => https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF#refs/tags/}/plugins-index.yaml"
